apiVersion: batch/v1
kind: CronJob
metadata:
  name: trf2-crawler
spec:
  schedule: "10 3 */2 * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: crawler
            image: us-docker.pkg.dev/inspira-registry/docker/data/crawlers:latest
            imagePullPolicy: Always
            command: [ "/bin/bash", "-c" ]
            args:
            - python commands.py trf2 --output-uri gs://inspira-production-buckets-trf2
            envFrom:
            - secretRef:
                name: crawler-env
            volumeMounts:
            - mountPath: "/etc/gcloud"
              name: gcp-credentials
          restartPolicy: Never
          volumes:
          - name: gcp-credentials
            secret:
              secretName: crawlers-credentials
