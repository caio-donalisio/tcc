apiVersion: batch/v1
kind: CronJob
metadata:
  name: tjmg-crawler
spec:
  schedule: "10 3 */2 * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: crawler
            image: us-docker.pkg.dev/inspira-registry/docker/data/crawlers:latest
            imagePullPolicy: Always
            command: [ "/bin/bash", "-c" ]
            args:
            - python commands.py tjmg --output-uri gs://inspira-production-buckets-tjmg
            envFrom:
            - secretRef:
                name: crawler-env
            volumeMounts:
            - mountPath: "/etc/gcloud"
              name: gcp-credentials
          restartPolicy: Never
          volumes:
          - name: gcp-credentials
            secret:
              secretName: crawlers-credentials
